{
  "name": "my-blog",
  "version": "1.0.0",
  "description": "A simple static blog",
  "scripts": {
    "build": "node build.js",
    "start": "npm run build && serve dist",
    "dev": "serve src"
  },
  "dependencies": {
    "js-yaml": "^4.1.0",
    "marked": "^9.0.0",
    "gray-matter": "^4.0.3"
  },
  "devDependencies": {
    "serve": "^14.2.4"
  }
}
